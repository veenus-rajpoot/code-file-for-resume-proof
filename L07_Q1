#include<iostream>
using namespace std;
int main combuian{{ ;
 g(908)
 my sewape :;
 left index(100)
 right index main program ;
 right program ;
 (1000);
 |||\\when two or more ethen two compound react ;
 using name space std;
 int main ();
 include<iostream>;
 };
 main program}